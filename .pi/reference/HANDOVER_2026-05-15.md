# Handover — 2026-05-15

## Scope completed

### 1. Pi migration
Converted Claude-specific project agent resources to pi-compatible resources.

#### Added
- `AGENTS.md`
- `.pi/README.md`
- `.pi/skills/elero-protocol/SKILL.md`
- `.pi/skills/esp32-development/SKILL.md`
- `.pi/skills/modern-cpp/SKILL.md`
- `.pi/skills/review-quality-gates/SKILL.md`
- `.pi/skills/sx1262-driver/SKILL.md`
- `.pi/reference/HANDOVER.md`
- `.pi/reference/TESTING_PLAN.md`

#### Removed
- `CLAUDE.md`
- `.claude/`

#### Updated references
- `docs/RFC-001-tx-race-and-logging.md`
- `docs/RFC-002-nvs-only-and-backup-restore.md`
- Project branch convention in agent docs changed from `claude/<session-id>` to `pi/<session-id>`

### 2. Dataflow simplification / robustness cleanup
Implemented a simplification-focused cleanup of the core dataflow.

#### Main changes
- Native NVS path now uses `NvsAdapter` as an `OutputAdapter`
- Removed shell polling via `last_notify_ms` / `last_changes`
- Shells now receive direct pushed updates through adapter callback path
- Removed surfaced `command_source` state from snapshots/publication
- Reduced hot-path RX/TX logging overhead in `components/elero/elero.cpp`
- Drained `tx_done_queue` before RX queue in `Elero::loop()`
- Made `RadioDriver::mode_` atomic
- Added checked posting for TX completion queue

#### Files changed
- `components/elero/elero.cpp`
- `components/elero/radio_driver.h`
- `components/elero/device.h`
- `components/elero/device_registry.h`
- `components/elero/device_registry.cpp`
- `components/elero/esp_cover_shell.h`
- `components/elero/esp_light_shell.h`
- `components/elero/state_snapshot.h`
- `components/elero/state_snapshot.cpp`
- `components/elero/cc1101_driver.cpp`
- `components/elero/sx1262_driver.cpp`
- `components/elero/sx1276_driver.cpp`
- `components/elero_mqtt/mqtt_adapter.cpp`
- `components/elero_nvs/__init__.py`
- `components/elero_nvs/nvs_adapter.h`

## Validation completed

Successful compile tests:

```bash
uv run esphome compile tests/test.esp32-minimal.yaml
uv run esphome compile tests/test.esp32-nvs.yaml
uv run esphome compile tests/test.esp32-mqtt.yaml
```

## Recommended next steps

### Highest-value cleanup still pending
1. Remove `CommandSource` end-to-end from `DeviceRegistry` / `Device` logic
2. Flatten `DeviceRegistry` command API where possible without changing RF semantics
3. Simplify `loop_cover_()` by extracting helper decisions or consolidating repeated enqueue/check patterns
4. Reassess whether raw RF adapter callback should stay in the common adapter contract or be isolated to web-only handling

### Suggested review order
1. Load `/skill:modern-cpp`
2. Load `/skill:esp32-development`
3. If touching RF protocol or radio behavior, also load `/skill:elero-protocol`
4. If touching SX1262, also load `/skill:sx1262-driver`
5. Before reviewing final changes, load `/skill:review-quality-gates`

## Important constraints
- ESP32 only
- No framework-specific split between ESP-IDF and Arduino unless already architecturally required
- Keep adapters thin
- Keep `DeviceRegistry` as single source of truth
- Prefer simplification/deletion over additive machinery where possible

## Notes for next session
- This repository now uses pi project resources (`AGENTS.md`, `.pi/skills/`)
- Historical references to `CLAUDE.md` still remain in `CHANGELOG.md` as historical changelog text and were intentionally not rewritten
